# Espelha o .dockerignore da raiz — necessário porque o build do compose
# usa ./frontend como contexto, e o Docker só lê o ignore da raiz DO CONTEXTO
# Git
.git
.gitignore

# Dependências
**/node_modules

# Ambiente e segredos — nunca entrar na imagem
**/.env
**/.env.*
